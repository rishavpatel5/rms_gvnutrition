{
  "permissions": {
    "allow": [
      "Bash(npx tsc -p tsconfig.json --noEmit)",
      "Bash(npx tsc -p tsconfig.app.json --noEmit)",
      "Bash(npx tsc -p tsconfig.build.json --noEmit)",
      "Bash(npx prisma validate)",
      "Bash(npm test)",
      "Bash(npm test -w @rms/api)"
    ]
  }
}
